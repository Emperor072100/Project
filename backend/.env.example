# ============================================
# VARIABLES DE ENTORNO PARA PRODUCCIÓN
# ============================================
# 
# ⚠️ IMPORTANTE: Estas variables tienen valores por defecto en el código
# pero es recomendable configurarlas en producción por seguridad
#
# Crear archivo .env en producción con:
# DATABASE_URL=postgresql+psycopg2://user:password@host:port/database
# SECRET_KEY=tu_clave_secreta_fuerte_y_aleatoria
# ACCESS_TOKEN_EXPIRE_MINUTES=60
# ALGORITHM=HS256
#
# ============================================

# Valores por defecto (cambiar en producción por seguridad)
DATABASE_URL=postgresql+psycopg2://user:password@host:5432/database
SECRET_KEY=change_this_to_a_strong_secret_key
ACCESS_TOKEN_EXPIRE_MINUTES=60
ALGORITHM=HS256
