# En producción usamos rutas relativas que serán manejadas por el proxy de Nginx
# Esto evita problemas de CORS y Mixed Content
# VITE_API_URL se deja vacío para que use '/api' automáticamente
VITE_API_URL=
